# NAC Quotation App

The production web/PWA source is prepared locally as `nac-quotation-app`. An Android APK requires an Android build toolchain (for example Trusted Web Activity/Capacitor/Android Studio). GitHub Actions can build the APK once the Android wrapper project is committed.
